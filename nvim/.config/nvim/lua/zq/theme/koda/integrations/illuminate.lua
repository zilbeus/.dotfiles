local colors = require("zq.theme.koda.palette")

return {
	IlluminatedWordRead = { fg = colors.base02, bg = colors.base05 },
	IlluminatedWordWrite = { fg = colors.base02, bg = colors.base05 },
	IlluminatedWordText = { underline = false, undercurl = false },
}
